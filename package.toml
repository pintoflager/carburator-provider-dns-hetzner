name = "hetzner"
version = "0.1.0"
collection = "DnsProvider"
platforms = ["Client", "Server"]

# List of provisioner names this provider supports
# provisioners = []
secret_names = ["hetzner_dns_apikey"]