name = "hetzner"
version = "0.1.0"
collection = "DnsProvider"
platforms = ["Client"]

# List of provisioner names this provider supports
# TODO: provider that calls api doesn't need provisioner that's called api...
# TODO: run api calls from provider itself.
# provisioners = ["api"]
secret_name = "hetzner_dns_apikey"